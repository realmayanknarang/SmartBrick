/**
 * client/src/tests/setup.js
 *
 * Vitest setup file — Phase 12A
 * ─────────────────────────────────────────────────────────────────────────────
 * Runs before each test file.  Imports @testing-library/jest-dom so that DOM
 * assertion matchers (.toBeInTheDocument, .toHaveValue, etc.) are globally
 * available in all React component tests without needing explicit imports.
 */
import '@testing-library/jest-dom';
